/**
 * Internal module for database queries
 **/


function connect() {
  return new Promise((accept, reject) => {
    reject();
  });
}

function addSurveyResponse(){
  return new Promise((accept, reject) => {
    reject();
  });
}

function getSurveyResponses(){
  return new Promise((accept, reject) => {
    reject();
  });
}

 export default { connect, addSurveyResponse, getSurveyResponses };
